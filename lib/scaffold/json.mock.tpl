/**
 * @file JSON模拟数据({{{url}}})
 * @author {{{author}}}({{{email}}})
 */

// 返回数据 E-JSON
var res = {status: 0};

module.exports = {
    type: '{{{data.type}}}',

    // 返回结果
    data: JSON.stringify(res)
};
